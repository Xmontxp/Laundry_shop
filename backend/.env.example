# Example env file for backend
PORT=4000
# Optional webhook URL to notify when machine has < 60 seconds remaining.
# If set to a LINE Notify webhook or any webhook URL, the server will POST JSON to it.
WEBHOOK_URL=
